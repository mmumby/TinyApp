<!DOCTYPE html>
<html lang="en">
<head>
  <% include ./partials/_header %>
  <title>TinyURL</title>
</head>
<body style="font-size:20px; padding:50px;">
<% if (!user) { %>
<% } else { %>
    <ul>
      <% for (let url in urls ) { %>
        <li><%= url %> - <%= urls[url].longURL %>
          <form method="POST" action="/urls/<%= url %>/delete">
          <button type="submit">Delete URL </button>
          <button><a href="/urls/<%= url %>">edit</a></button>
          </form>
        </li>
      <% }; %>
    </ul>
  <h4>Create a new<a href="/urls/new"> ShortURL!</a>  </h4>
<% }; %>
</body>
</html>